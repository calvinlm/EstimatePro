{
  "name": "estimatepro-ph",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace frontend && npm run build --workspace server",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace server",
    "format": "npm run format --workspace frontend && npm run format --workspace server",
    "format:check": "npm run format:check --workspace frontend && npm run format:check --workspace server"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
